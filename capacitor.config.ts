import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sistemaadministrativo.revisorcxp',
  appName: 'Revisor CxP',
  webDir: 'www',
};

export default config;
